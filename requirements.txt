# ─────────────────────────────────────────────────────────────
# Gesture Media Controller - Requirements 
# Install with: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

opencv-python       # Camera capture & UI rendering
mediapipe           # Hand landmark detection

# NOTE: No extra packages needed for volume/media control.
# built for windows / linux / macos
# built into windows — pip install required.
made by SHIVANSH KUMAR
